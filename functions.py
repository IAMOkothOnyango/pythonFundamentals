# A function is a block of code which only runs when it is called. You can pass data, known as parameters, into a function. A function can return data as a result.

def sayHi(name, age): # this defines a function called sayHi which takes in a parameter called name and age (see below)
    print("Hello " + name + " you are " + str(age) + " years old. How about that?") # this prints "Hello" and the name and age parameter to the console (terminal) (see below)
    
sayHi("Okoth", 28) # this calls the function sayHi and prints "Hello Okoth" to the console (terminal) (see above)
sayHi("User", 50) # this calls the function sayHi and prints "Hello User" to the console (terminal) (see above)
sayHi("John", 36) # this calls the function sayHi and prints "Hello John" to the console (terminal) (see above)